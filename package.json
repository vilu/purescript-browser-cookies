{
    "private": true,
    "scripts": {
        "clean": "rimraf output && rimraf .pulp-cache",
        "build": "eslint src && pulp build -- --strict && pulp build --to dist/browser-cookies.js",
        "watch": "pulp -w build --to dist/browser-cookies.js",
        "test": "pulp test",
        "watch-test": "pulp -w test",
        "repl": "pulp repl"
    },
    "devDependencies": {
        "eslint": "^5.6.0",
        "pulp": "^12.3.0",
        "purescript": "^0.12.0",
        "purescript-psa": "^0.7.2",
        "rimraf": "^2.6.2"
    }
}
